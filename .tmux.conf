set -g default-terminal "screen-256color"
set -ga terminal-overrides ',xterm-256color:Tc'
# set -g default-terminal "tmux-256color"

set -g status-position bottom 

set -g prefix C-a
unbind C-b
bind-key C-a send-prefix

unbind %
bind | split-window -h -c '#{pane_current_path}'

unbind '"'
bind - split-window -v -c '#{pane_current_path}'

unbind r
bind r source-file ~/.tmux.conf

unbind s

bind -r j resize-pane -D 5
bind -r k resize-pane -U 5
bind -r l resize-pane -R 5
bind -r h resize-pane -L 5

bind -r m resize-pane -Z

set -g mouse on

set-window-option -g mode-keys vi

bind-key -T copy-mode-vi 'v' send -X begin-selection # start selecting text with "v"
bind-key -T copy-mode-vi 'y' send -X copy-selection # copy text with "y"

bind-key -r S run-shell "tmux-sessionizer ~/dev/frontend/apps/webapp-studio"
bind-key -r F run-shell "tmux-sessionizer ~/dev/frontend"
bind-key -r A run-shell "tmux-sessionizer ~/dev/frontend/packages/react-auth"
bind-key -r L run-shell "tmux-sessionizer ~/dev/frontend/apps/webapp-login"
bind-key -r D run-shell "tmux-sessionizer ~/dev/personal/.dotfiles"

unbind -T copy-mode-vi MouseDragEnd1Pane # don't exit copy mode when dragging with mouse

# yazi
set -g allow-passthrough on

set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM

# remove delay for exiting insert mode with ESC in Neovim
set -sg escape-time 10

# tpm plugin
set -g @plugin 'tmux-plugins/tpm'

set -g @plugin 'christoomey/vim-tmux-navigator'
set -g @plugin 'jimeh/tmux-themepack'
set -g @plugin 'tmux-plugins/tmux-resurrect' # persist tmux sessions after computer restart
set -g @plugin 'tmux-plugins/tmux-continuum' # automatically saves sessions for you every 15 minutes

set -g @continuum-boot 'on'

# CATTPUCCIN THEME SETUP
# set -g @plugin 'catppuccin/tmux'

# KANAGAWA THEME SETUP
# set -g @plugin 'Nybkox/tmux-kanagawa'
# set -g @kanagawa-plugins "ram-usage time"
# set -g @kanagawa-show-timezone false
# set -g @kanagawa-military-time true
# set -g @kanagawa-show-powerline true
# set -g @kanagawa-show-left-sep 
# set -g @kanagawa-show-right-sep 
# set -g @kanagawa-show-flags true
#
# set -g window-active-style 'fg=default,bg=default'
# set -g window-style 'fg=default,bg=default'

# GRUVBOX THEME SETUP
set -g @plugin 'egel/tmux-gruvbox'

set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'

# set -g mode-style "fg=#8aa2f7,bg=#3b4261"
#
# set -g message-style "fg=#ffc777,bg=#3b4261"
# set -g message-command-style "fg=#ffc777,bg=#3b4261"
#
# set -g pane-border-style "fg=#3b4261"
# set -g pane-active-border-style "fg=#ffc777"
#
# set -g status "on"
# set -g status-justify "left"
#
# set -g status-style "fg=#ffc777,bg=#1f2335"
#
# set -g status-left-length "100"
# set -g status-right-length "100"
#
# set -g status-left-style NONE
# set -g status-right-style NONE
#
# set -g status-left "#[fg=#1d202f,bg=#ffc777,bold] #S #[fg=#ffc777, bg=#1f2335 ,nobold,nounderscore,noitalics]"
# set -g status-right "#[fg=#1f2335,bg=#1f2335,nobold,nounderscore,noitalics]#[fg=#ffc777,bg=#1f2335] #{prefix_highlight} #[fg=#3b4261,bg=#1f2335,nobold,nounderscore,noitalics]#[fg=#ffc777,bg=#3b4261] %Y-%m-%d  %I:%M %p #[fg=#ffc777,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=#1d202f,bg=#ffc777,bold] #(id -F) "
#
# if-shell '[ "$(tmux show-option -gqv "clock-mode-style")" == "24" ]' {
#   set -g status-right "#[fg=#1f2335,bg=#1f2335,nobold,nounderscore,noitalics]#[fg=#9d7cd8,bg=#1f2335] #{prefix_highlight} #[fg=#3b4261,bg=#1f2335,nobold,nounderscore,noitalics]#[fg=#9d7cd8,bg=#3b4261] %Y-%m-%d  %H:%M #[fg=#9d7cd8,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=#1d202f,bg=#9d7cd8,bold] #(id -F) "
# }
#
# setw -g window-status-activity-style "underscore,fg=#a9b1d6,bg=#1f2335"
# setw -g window-status-separator ""
# setw -g window-status-style "NONE,fg=#a9b1d6,bg=#1f2335"
# setw -g window-status-format "#[fg=#1f2335,bg=#1f2335,nobold,nounderscore,noitalics]#[default] #I  #W #F #[fg=#1f2335,bg=#1f2335,nobold,nounderscore,noitalics]"
# setw -g window-status-current-format "#[fg=#1f2335,bg=#3b4261,nobold,nounderscore,noitalics]#[fg=#ffc777,bg=#3b4261,bold] #I  #W #F #[fg=#3b4261,bg=#1f2335,nobold,nounderscore,noitalics]"

# tmux-plugins/tmux-prefix-highlight support
# set -g @prefix_highlight_output_prefix "#[fg=#e0af68]#[bg=#1f2335]#[fg=#1f2335]#[bg=#e0af68]"
# set -g @prefix_highlight_output_suffix ""

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
