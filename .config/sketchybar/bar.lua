local colors = require("colors")

sbar.bar({
	topmost = "window",
	height = 36,
	color = colors.bar.bg,
	padding_right = 2,
	padding_left = 2,
	margin = 0,
	corner_radius = 0,
	y_offset = 0,
	border_color = colors.transparent,
	border_width = 0,
	blur_radius = 10,
})
